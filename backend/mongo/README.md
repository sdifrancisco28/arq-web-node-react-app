El Dockerfile de este directorio buildea la imagen para el backend: arq-web-mongodb-img

## Buildear con el comando
docker build -t arq-web-mongodb-img:[tag] .

# Reemplazar tag por la versión de la imagen
