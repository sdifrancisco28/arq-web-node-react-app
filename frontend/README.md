El Dockerfile de este directorio buildea la imagen para el frontend: arq-web-frontend-img

## Buildear con el comando
docker build -t arq-web-frontend-img:[tag] .

# Reemplazar tag por la versión de la imagen